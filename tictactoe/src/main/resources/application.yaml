spring:
  application:
    name: tictactoe
  datasource:
    url: jdbc:postgresql://localhost:5432/tictactoe
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
